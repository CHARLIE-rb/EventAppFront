import 'package:flutter/material.dart';

class ImageLikePint {
  final String imageUrl;
  final Alignment align;

  ImageLikePint({required this.imageUrl, this.align = Alignment.center});
  // : image = Image.asset(imageUrl);
}
